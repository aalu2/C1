#include <stdio.h>   // This line includes the standard input/output library

int main() {
    int x=5, y=6, add;   // Declaring variables x, y and add and initializing x and y with values 5 and 6 respectively
    add=x+y;              // Assigning the value of x+y to the variable add
    printf("%d",add);    // Printing the value of add using printf function
    return 0;            // Returning 0 to indicate that the program ran successfully
}
